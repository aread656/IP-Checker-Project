image: gradle:8.7-jdk21
stages:
  - build
  - test

cache:
  paths:
    - .gradle/

build:
  stage: build
  script:
    - gradle build -x test
  artifacts:
    paths:
      - build/
test:
  stage: test
  script:
    - gradle test
